name: cogo_chat_sdk_flutter
description: Flutter/Dart SDK for COGO chat protocol over Supabase Edge.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  http: ^1.2.2
  web: ^1.0.0
  meta: ^1.12.0
  json_annotation: ^4.9.0
  supabase_flutter: ^2.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.7
  build_runner: ^2.4.10
  json_serializable: ^6.9.0
